set nobeep
set autolist
set prompt='%m:%c%# '
alias ls /bin/ls --color=auto
setenv TERM screen-256color
eval `dircolors ~/.dircolors`

setenv EDITOR vim
setenv VISUAL vim
setenv INPUTRC ~/.inputrc

# set tty erase to backspace or delete
stty erase 
